<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".event_details">

    <TextView  android:id="@+id/noticeTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="notice Title"
        android:textColor="@color/white"
        android:textStyle="bold"
        android:textSize="24dp"
        android:layout_margin="8dp"
        android:background="@drawable/rounded_10dp_blue"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/noticeImage"
        />

    <ImageView android:id="@+id/noticeImage"
        android:layout_width="0dp"
        android:layout_height="150dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        tools:srcCompat="@tools:sample/avatars"
        app:layout_constraintTop_toBottomOf="@id/noticeTitle"
        app:layout_constraintStart_toStartOf="@id/noticeTitle"
        app:layout_constraintEnd_toEndOf="@id/noticeTitle"
        />

    <TextView  android:id="@+id/noticeDetails"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="noticeDetails"
        android:textColor="@color/white"
        android:textStyle="bold"
        android:textSize="16dp"
        android:layout_margin="8dp"
        android:background="@drawable/rounded_10dp_dark_blue"
        app:layout_constraintTop_toBottomOf="@id/noticeImage"
        app:layout_constraintStart_toStartOf="@id/noticeTitle"
        app:layout_constraintEnd_toEndOf="@id/noticeTitle"
        />



</androidx.constraintlayout.widget.ConstraintLayout>