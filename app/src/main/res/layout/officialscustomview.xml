<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp">

    <TextView
        android:id="@+id/offid"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="offid"
        app:layout_constraintEnd_toStartOf="@+id/officialsImage"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/offname"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="offname"
        app:layout_constraintEnd_toStartOf="@+id/officialsImage"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/offid" />

    <TextView
        android:id="@+id/offdepartment"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="offdepartment"
        app:layout_constraintEnd_toStartOf="@+id/officialsImage"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/offname" />

    <ImageView
        android:id="@+id/officialsImage"
        android:layout_width="100dp"
        android:layout_height="100dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>